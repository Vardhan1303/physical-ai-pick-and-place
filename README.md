# physical-ai-pick-and-place
MuJoCo simulation of a Franka arm that picks and places objects using ArUco-marker point prompts segmented by FLIP (Fovea-Like Input Patching) — no shape-specific training. Classifies square, circle, and triangle objects from the segmented mask's geometry and sorts them into matching bins.
